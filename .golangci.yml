version: "2"
run:
  timeout: 5m
  go: "1.26.5"
  build-tags:
    - goexperiment.arenas
    - goexperiment.goroutineleakprofile
    - goexperiment.jsonv2
    - goexperiment.runtimesecret
    - goexperiment.simd
  allow-parallel-runners: true
  allow-serial-runners: true
  issues-exit-code: 1
  tests: true
output:
  formats: {}
linters:
  enable:
    - arangolint
    - asasalint
    - asciicheck
    - bidichk
    - bodyclose
    - canonicalheader
    - clickhouselint
    - containedctx
    - contextcheck
    - copyloopvar
    - cyclop
    - decorder
    - depguard
    - dogsled
    - dupl
    - dupword
    - durationcheck
    - embeddedstructfieldcheck
    - err113
    - errcheck
    - errchkjson
    - errname
    - errorlint
    - exhaustive
    - exhaustruct
    - exptostd
    - fatcontext
    - forbidigo
    - forcetypeassert
    - funlen
    - ginkgolinter
    - gocheckcompilerdirectives
    - gochecknoglobals
    - gochecknoinits
    - gochecksumtype
    - gocognit
    - goconst
    - gocritic
    - gocyclo
    - godoclint
    - godot
    - godox
    - goheader
    - gomoddirectives
    - gomodguard_v2
    - goprintffuncname
    - gosec
    - gosmopolitan
    - govet
    - grouper
    - iface
    - importas
    - inamedparam
    - ineffassign
    - interfacebloat
    - intrange
    - iotamixing
    - ireturn
    - loggercheck
    - maintidx
    - makezero
    - mirror
    - misspell
    - mnd
    - modernize
    - musttag
    - nakedret
    - nestif
    - nilerr
    - nilnesserr
    - nilnil
    - nlreturn
    - noctx
    - nolintlint
    - nonamedreturns
    - nosprintfhostport
    - paralleltest
    - perfsprint
    - prealloc
    - predeclared
    - promlinter
    - protogetter
    - reassign
    - recvcheck
    - revive
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - staticcheck
    - tagalign
    - tagliatelle
    - testableexamples
    - testifylint
    - testpackage
    - thelper
    - tparallel
    - unconvert
    - unparam
    - unqueryvet
    - unused
    - usestdlibvars
    - usetesting
    - varnamelen
    - wastedassign
    - whitespace
    - wrapcheck
    - wsl_v5
    - zerologlint
  settings:
    cyclop:
      max-complexity: 12
    depguard:
      rules:
        main:
          allow:
            - $gostd
            - $module
            # CLI framework and test assertions used throughout internal/cli and cmd/
            - github.com/spf13/cobra
            - github.com/stretchr
            # Private dependencies: go-finding, go-atomic-write, go-error-family
            - github.com/larsartmann
            - github.com/LarsArtmann
    exhaustruct:
      exclude:
        - os/exec.Cmd
    ginkgolinter:
      forbid-focus-container: true
      forbid-spec-pollution: true
    goconst:
      ignore-tests: true # Test fixtures often repeat strings deliberately
    gocritic:
      disabled-checks:
        - ifElseChain
    gomoddirectives:
      replace-local: true
    makezero:
      always: true
    revive:
      rules:
        - disabled: true
          name: exported
        - disabled: true
          name: package-comments
    tagliatelle:
      case:
        rules:
          json: snake # .oxlintrc.json uses snake_case field names
    testifylint:
      disable:
        - go-require
      enable-all: true
    ireturn:
      allow:
        - error
        - empty
        - anon
        - stdlib
        - generic
    varnamelen:
      ignore-map-index-ok: true
      ignore-type-assert-ok: true
      ignore-names:
        # Go stdlib conventions
        - err # error return value
        - ok # map/type assertion ok value
        - fn # function/callback parameter
        - w # io.Writer parameter (fmt.Fprintf, io.Copy, etc.)
        - r # io.Reader/rule/registry variable
        - s # string parameter
        # Loop and test conventions
        - i # loop index
        - t # *testing.T parameter
        - tt # table test case
        # Domain-specific short names (consistent across codebase)
        - p # profile/plugin/path
        - f # finding/flag
        - d # decision/data
        - c # category/command/config
        - n # number/count
        - b # bool/buffer
        - k # map key in range loops
  exclusions:
    generated: lax
    rules:
      - text: cobra.Command is missing
        linters:
          - exhaustruct
      - text: slog[.].*is missing
        linters:
          - exhaustruct
      - text: exec[.]ExitError is missing
        linters:
          - exhaustruct
      - text: finding[.]Position is missing
        linters:
          - exhaustruct
      - text: config[.]OxlintConfig is missing
        linters:
          - exhaustruct
      - text: rule[.]Rule is missing
        linters:
          - exhaustruct
      - text: oxlint[.]
        linters:
          - exhaustruct
      - text: format[.]FindingView is missing
        linters:
          - exhaustruct
      - text: pipeline[.]PipelineResult is missing
        linters:
          - exhaustruct
      - text: finding[.]ToolInfo is missing
        linters:
          - exhaustruct
      - text: G204
        source: exec.Command
        linters:
          - gosec
      - text: G304
        linters:
          - gosec
      - path: _test[.]go$
        text: G306
        linters:
          - gosec
      - path: _test\.go
        linters:
          - exhaustruct
          - testpackage
          - gochecknoglobals
          - funlen
          - cyclop
          - goconst
      - path: _test\.go
        text: unused
        linters:
          - unused
      - path: internal/cli/cmd_.*\.go$
        linters:
          - forbidigo # CLI commands use fmt.Println/Printf for stdout output by design
      - path: _test\.go
        linters:
          - err113 # Test errors are ad-hoc, not programmatic contracts
          - mnd # Test magic numbers are idiomatic (slice lengths, positions, etc.)
      - text: devDependencies
        linters:
          - tagliatelle # camelCase field in package.json, not our config output
    paths:
      - \.gen\.go$
      - _templ\.go$
      - vendor/
formatters:
  enable:
    - gci
    - goimports
    - gofumpt
    - golines
    - swaggo
  settings:
    golines:
      max-len: 120
  exclusions:
    generated: lax
    paths:
      - _templ\.go$
issues:
  max-issues-per-linter: 50
  max-same-issues: 10
